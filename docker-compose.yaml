services:
  marstack:
    build:
      dockerfile: Dockerfile
      context: .
    restart: always
    ports:
      - "80:8000"
    environment:
      - LOG_LEVEL=info
      - APP_PORT=8000
      - APP_TIMEZONE=Europe/Berlin
